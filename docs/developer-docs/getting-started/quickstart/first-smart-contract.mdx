---
keywords: [beginner, getting started, first smart contract, first deployment, deploy a smart contract, quickstart, quick start]
---

import useBaseUrl from "@docusaurus/useBaseUrl";
import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";
import { GlossaryTooltip } from "/src/components/Tooltip/GlossaryTooltip";
import TabItem from "@theme/TabItem";
import { AdornedTabs } from "/src/components/Tabs/AdornedTabs";

# Quick start

<MarkdownChipRow labels={["Beginner", "Getting started" ]} />

Build Web3, DeFi, fully network-resident social media, games, enterprise apps, and just about anything else you can imagine. Your projects are immune to cyber attacks and unstoppable.

**Deploy in minutes using these steps.**

<AdornedTabs>
<TabItem value="linux" label="Linux" default={true}>

#### Prerequisites

- [ ] Download and [install Node.js](https://nodejs.org/en/download/package-manager).

#### 1. Install the IC SDK.

```bash
sh -ci "$(curl -fsSL https://internetcomputer.org/install.sh)"
source "$HOME/.local/share/dfx/env"
```

Confirm the IC SDK has been installed:

```bash
dfx --version
```

#### 2. Start `dfx`.

```bash
dfx start --background
```

#### 3. Create a new project.

The default project created by `dfx` is "Hello, world!"


```bash
dfx new hello --type=motoko 
```

Creates a new Motoko project. Options are [motoko, rust, azle, kybra]

#### 4. Deploy your project.

```bash
cd hello
dfx deploy --playground
```

#### 5. Interact with your project.

Open the URL in your web browser that `dfx` returned after deploying your project.

</TabItem>
<TabItem value="macOS" label="macOS">

#### Prerequisites

- [ ] Download and [install Node.js](https://nodejs.org/en/download/package-manager). Using [HomeBrew](https://brew.sh/) is recommended.

- [ ] Apple silicon machines: Download and install Rosetta: `softwareupdate --install-rosetta`

#### 1. Install the IC SDK.

```bash
sh -ci "$(curl -fsSL https://internetcomputer.org/install.sh)"
source "$HOME/.local/share/dfx/env"
```

Confirm the IC SDK has been installed:

```bash
dfx --version
```

#### 2. Start `dfx`.

```bash
dfx start --background
```

#### 3. Create a new project.

The default project created by `dfx` is "Hello, world!"

```bash
dfx new hello --type=motoko
```

Creates a new Motoko project. Options are [motoko, rust, azle, kybra]

#### 4. Deploy your project.

```bash
cd hello
dfx deploy --playground
```

#### 5. Interact with your project.

Open the URL in your web browser that `dfx` returned after deploying your project.

</TabItem>

<TabItem value="windows" label="Windows">

#### Prerequisites

- [ ] Download and install [Windows Subsystem for Linux](https://docs.microsoft.com/en-us/windows/wsl/install).

- [ ] Download and [install Node.js](https://nodejs.org/en/download/package-manager) within your WSL environment.

#### 1. Install the IC SDK.

```bash
sh -ci "$(curl -fsSL https://internetcomputer.org/install.sh)"
source "$HOME/.local/share/dfx/env"
```

Confirm the IC SDK has been installed:

```bash
dfx --version
```

#### 2. Start `dfx`.

```bash
dfx start --background
```

#### 3. Create a new project.

The default project created by `dfx` is "Hello, world!"

```bash
dfx new hello --type=motoko
```

Creates a new Motoko project. Options are [motoko, rust, azle, kybra]

#### 4. Deploy your project.

```bash
cd hello
dfx deploy --playground
```

#### 5. Interact with your project.

Open the URL in your web browser that `dfx` returned after deploying your project.

</TabItem>

</AdornedTabs>


*Congratulations, you just created your first internet-native dapp!*
