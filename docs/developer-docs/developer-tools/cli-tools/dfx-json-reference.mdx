---
keywords: [reference, dfx, dfx.json schema]
---

import { MarkdownChipRow } from "/src/components/Chip/MarkdownChipRow";

# dfx.json schema

<MarkdownChipRow labels={["Reference"]} />

## Overview

This is the schema reference for `dfx.json` in [JSON Schema](https://json-schema.org) format.

To download the schema, save [this file](./dfx-json-schema.json).

To generate the schema to a file yourself, use `dfx schema --outfile path/to/outfile.json` as described in the [`dfx schema` reference](/docs/current/developer-docs/developer-tools/cli-reference/dfx-schema.mdx).

``` json file=./dfx-json-schema.json
```