canisters:
  - name: portal
    recipe:
      type: '@dfinity/asset-canister@v2.1.0'
      configuration:
        version: 0.29.2
        build:
          - npm install
          - npm run build
        dir: build