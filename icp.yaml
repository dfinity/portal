canisters:
  - name: portal
    recipe:
      type: '@dfinity/asset-canister'
      configuration:
        version: 0.29.2
        build:
          - npm install
          - npm run build
        dir: build