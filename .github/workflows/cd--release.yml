on:
  pull_request:
    branches:
      - master
    types: [closed]

jobs:
  deploy:
    name: Deploy to IC
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    
    steps: 
      - run: echo "Triggered"
